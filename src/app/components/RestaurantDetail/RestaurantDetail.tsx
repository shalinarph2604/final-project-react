import React from 'react';

const RestaurantDetail = () => {
    return (
        <div>

        </div>
    )
}

export default RestaurantDetail

// List all dishes
// Add to cart button
// Cart menu (doesn't have to be floating)
// Cart must be responsive with the current cart state (dish quantity, prices, etc)
// Paginated (Optional)
// Show before after discounted price (Optional) e.g => $200(strikethrouged) $150